import AdminShell from "@/components/AdminShell";

/** /workflows resolves to the same dashboard-first admin shell as the landing. */
export default function WorkflowsPage() {
  return <AdminShell />;
}
