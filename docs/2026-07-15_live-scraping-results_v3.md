# Live Admin Console Architectural Scan — admin-test.landjourney.ai

Generated 2026-07-15 via headful Playwright scan for Workflow Creator integration.

**Security note:** Authorization/token header values and localStorage token values are masked (first 8 / last 4 chars shown with length) rather than recorded in full, to avoid persisting live session credentials in this file. Payload fields matching common PII patterns (SSN, DOB, email, phone, etc.) are similarly masked. Header/field *names* and JSON structure are captured in full.

## 1. HTTP Interceptor Header Audit

_No requests to the API host were observed. Check that login succeeded within the 60s window and that the app makes calls from /home._

## 2. IAM User & Role Structure

```json
null
```

## 3. App Shell, Router, & Feature Flags

### DOM outline
```json
null
```

### localStorage (sensitive values masked)
```json
null
```

## 4. CSS & Design System Variables

### Root CSS custom properties
```json
null
```

### Computed styles on sampled elements
```json
null
```

## 5. Dynamic Form & Document Checklist Templates

### Form template schema (first captured)
```json
null
```

### Checklist / file template endpoints observed
```json
[]
```

## 6. Request Lifecycle & Database Attributes

```json
null
```

## Issues / Manual Follow-up Needed

None recorded.
