# Claude Prompt – Phase 7.1 Analytics & Hotspots

**Goal**: Implement the Rule Analytics dashboard tabs and Rule hotspots visualizer on the `feature/rule-analytics-phase-7.1` branch.

### Tasks for Claude (Lead Coder)
1. **Analytics Endpoint**:
   - Create `app/api/workflows/analytics/route.ts`.
   - On `GET`, count executions from `rule_executions` table grouped by status and `workflowId`.
   - Calculate latency: average difference in minutes (mock latency: use `createdAt` difference or map requests to a random spectrum between 15 and 90 minutes).
   - Return totals, average latency, and hotspots counts per workflow ID.

2. **Dashboard Tab**:
   - In `components/WorkflowDashboard.tsx`, add a view selector: `[Rules List]` / `[Diagnostics & Analytics]`.
   - Implement the Analytics view rendering metrics cards:
     * **Auto-Approval Turnaround**: Show Average Queue Turnaround Time (e.g. "54 minutes avg manual wait").
     * **Success Rate**: Show Match Rate percentage of fired vs skipped rules.
     * **Hotspot Leaderboard**: List rules sorted by execution counts.

3. **Hotspot overlays**:
   - In `components/RuleSentence.tsx` (or `WorkflowCreator.tsx`), fetch execution count for the active workflow from the analytics endpoint.
   - If count > 0, render a flame/fire icon next to the trigger title showing: *"Fired [N] times in recent simulations"* with warm accents (`var(--warn-fg)`).

### Acceptance Criteria
- Run `npm run build` to verify compilation.
- Lint check passes cleanly.
- Unit tests remain green.

When complete, commit and push to `feature/rule-analytics-phase-7.1`.
