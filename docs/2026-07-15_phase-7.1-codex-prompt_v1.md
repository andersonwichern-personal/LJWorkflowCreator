# OpenAI / Codex Prompt – Phase 7.1 Scaffolding

**Goal**: Support Claude in inline autocompletions for Phase 7.1 Analytics.

## Deliverables
1. **API Router Skeleton**:
   - `app/api/workflows/analytics/route.ts` using Prisma client to aggregate counts from `rule_executions`.
2. **Analytics View Layout**:
   - Tab rendering logic inside `components/WorkflowDashboard.tsx`.
   - Tailwind styling for the metrics cards (turnaround time, match rate, leaderboard list).
3. **Hotspot Overlay Element**:
   - Flame/fire icon component insertion logic next to trigger headers in `components/RuleSentence.tsx`.

## Instructions
- Provide TypeScript React structures.
- Use Lucide icons (`Flame`, `TrendingUp`, `Clock`).
